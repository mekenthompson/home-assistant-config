# Shell commands for Home Assistant
# Run addon health check with HA notifications
addon_health_check: "/data/openclaw-home/bin/addon-health-check --ha-notify --verbose >> /config/addon-health-check.log 2>&1"
